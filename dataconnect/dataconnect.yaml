specVersion: "v1beta"
serviceId: "artisan-platform"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "artisan-platform-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
